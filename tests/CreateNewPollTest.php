<?php

use Illuminate\Foundation\Testing\WithoutMiddleware;
use Illuminate\Foundation\Testing\DatabaseMigrations;
use Illuminate\Foundation\Testing\DatabaseTransactions;

class CreateNewPollTest extends TestCase
{/*
    public function testIgnoreAllFieldsStep1()
    {
        $this->visit('/')
            ->click(trans('polls.button.continue'))
            ->seePageIs('/');
    }

    public function testIgnoreAllFieldsStep2()
    {
        $faker = Faker\Factory::create();
        $this->visit('/')
            ->type($faker->name, 'name')
            ->type($faker->text, 'title')
            ->type($faker->email, 'email')
            ->click(trans('polls.button.finish'))
            ->seePageIs('/');
    }

    public function testIgnoreAllFieldsStep3()
    {
        $faker = Faker\Factory::create();
        $this->visit('/')
            ->type($faker->name, 'name')
            ->type($faker->text, 'title')
            ->type($faker->email, 'email')
            ->click(trans('polls.button.continue'))
            ->click(trans('polls.button.continue'))
            ->seePageIs('/');
    }*/
}
